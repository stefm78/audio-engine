# Resource admissibility amendment history

2026-08-26 — introduced after the RVC learned-character track reached its first training boundary and the required user-provided/self-hosted CUDA resource was confirmed unavailable.

The amendment changes execution governance only. It does not invalidate the RVC `dataset-pass`, the frozen GPU package, or any prior scientific result.
